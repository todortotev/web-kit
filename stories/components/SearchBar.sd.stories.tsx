import React from "react";
import SearchBar from '../../src/components/SearchBar_sd';

export default { title: 'SearchBar.sd' };

export const Scene = () => (
  <>
    <SearchBar />
    <style>{`body { margin : 0; font-family: Helvetica}`}</style>
  </>
);
